"""Utility to clean responses/keys/values"""


def clean_dict(dict_format: dict[str, str]) -> str:
    """Return key or value in string format"""

    for str_val in dict_format:
        return str_val
